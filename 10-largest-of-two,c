#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the values:");
    scanf("%d %d",&a,&b);
    if(a>b)
        printf("%d is larger than %d",a,b);
    else
        printf("%d is larger than %d",b,a);
    return 0;
}
